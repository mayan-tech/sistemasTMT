<h3>ASIGNAR USUARIO EMPRESA Y DEPARTAMENTO
   <small>Lista de ASIGNACIONES creadas en el sistema.</small>
</h3>
<p id="notice"><%= notice %></p>
<div class="container-fluid">
   <!-- START DATATABLE 1 -->
   <div class="row">
      <div class="col-lg-12">
         <div class="panel panel-default">
            <div class="panel-heading">
               Lista |
               <small>Asignaciones</small>
            </div>
            <div class="panel-body">
<table id="datatable1" class="table table-striped table-hover"id="datatable1" class="table table-striped table-hover">
  <thead>
    <tr>
      <th>User</th>
      <th>Departament</th>
      <th>Company</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @asign_departament_users.each do |asign_departament_user| %>
      <tr>
        <td><%= asign_departament_user.user.first_name %> <%= asign_departament_user.user.last_name %></td>
        <td><%= asign_departament_user.departament.name %></td>
        <td><%= asign_departament_user.company.name %></td>
        <td><%= link_to 'Show', asign_departament_user %></td>
        <td><%= link_to 'Edit', edit_asign_departament_user_path(asign_departament_user) %></td>
        <td><%= link_to 'Destroy', asign_departament_user, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<%= link_to 'Nueva Asignación ', new_asign_departament_user_path, class: 'btn btn-primary' %>
</div>

</div>
<!-- END DATATABLE 1 -->


</div>
</div>
