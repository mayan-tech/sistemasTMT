<h3>CATEGORIAS
   <small>Lista de categorias creadas en el sistema.</small>
</h3>
<p id="notice"><%= notice %></p>
<div class="container-fluid">
   <!-- START DATATABLE 1 -->
   <div class="row">
      <div class="col-lg-12">
         <div class="panel panel-default">
            <div class="panel-heading">
               Lista |
               <small>Categorias</small>
            </div>
            <div class="panel-body">
              <table id="datatable1" class="table table-striped table-hover">
                 <thead>

                    <tr>
                      <th  class="sort-numeric">Name</th>

                      <th colspan="3">Acciones</th>
                 </thead>
                 <tbody>
                   <% @categories.each do |category| %>
                     <tr class="gradeX">
                       <td colspan="3"><%= category.name %></TD>
                       <td colspan="3">
                       <%= link_to 'Mostrar', category, class:'btn btn-info' %>
                       <%= link_to 'Editar', edit_category_path(category), class:'btn btn-green' %>
                       <%= link_to 'Eliminar', category, class:'btn btn-danger', method: :delete, data: { confirm: '¿ Esta seguro de eliminar ' + category.name + '?' } %></td>
                     </tr>
                   <% end %>
                 </div>
              </div>
               </table>
                 <%= link_to 'Nueva categoria', new_category_path, class: 'btn btn-primary' %>
             </div>

             </div>
             <!-- END DATATABLE 1 -->


             </div>
              </div>
